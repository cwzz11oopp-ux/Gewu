# Security policy

Report security issues privately to the repository owner instead of opening a
public issue. Never include API keys, SSH keys, private datasets, uploaded
papers, or generated experiment artifacts in a report.

The project executes generated experiment code only through its configured
local or SSH experiment provider. Review provider configuration and generated
bundles before using the project with sensitive infrastructure.
